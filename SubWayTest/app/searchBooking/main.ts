import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { SearchBookingModule } from './searchBooking.module';

platformBrowserDynamic().bootstrapModule(SearchBookingModule);